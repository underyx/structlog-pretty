machine:
  python:
    version: '3.5.2'

dependencies:
  override:
    - pip install -e .[fast]
    - pip install -r test-requirements.txt

test:
  override:
    - pytest --cov
  post:
    - bash <(curl -s https://codecov.io/bash)

